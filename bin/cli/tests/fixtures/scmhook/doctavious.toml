[scmhook_settings]
[scmhook_settings.hooks.pre-commit]
name = "pre-commit"
[scmhook_settings.hooks.pre-commit.executions.format-backend]
name = "format-backend"
type = "command"
run = "cargo fmt"
root = "backend"

# need a files test
# need a script test
# need a skip test
# need a only test
# need a root test
# need a parallel test
# need a exclude tag
# need a test for variable replacement